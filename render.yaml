services:
  - type: web
    name: boardgame-ai
    env: docker
    plan: free
    envVars:
      - key: JWT_SECRET_KEY
        generateValue: true
      - key: GEMINI_API_KEY
        sync: false
      - key: ENV
        value: production
      - key: DEBUG
        value: "false"
